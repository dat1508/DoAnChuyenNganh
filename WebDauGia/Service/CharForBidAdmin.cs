using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using WebDauGia.Helper.Excel;
using WebDauGia.Models;

namespace WebDauGia.Service
{
    public class CharForBidAdmin
    {
        /*public static List<ExportBid> RegisterDetail(int id)
        {
            using (var db = new DBContext())
            {
                var listDetail = db.BID.Where(x =>x.Id == id)
                                                                  .Select(x => new ExportBid
                                                                  {
                                                                      DateCreate = (DateTime)x.CreateDate,
                                                                       BidPrice = (int)x.BidPrice,

                                                                  }).ToList();

                return listDetail;
            }
        }*/
    }
}